#include <stdio.h>
#include <stdlib.h>

/**
 * main - Prints all digits of base 10 for 0
 *
 * Return: Always 0(Sucess)
*/

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
